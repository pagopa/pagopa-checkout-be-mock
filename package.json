{
  "name": "io-pagopa-pm-mock",
  "version": "0.0.1",
  "description": "TODO",
  "repository": "https://github.com/pagopa/io-pagopa-pm-mock",
  "author": "https://pagopa.gov.it",
  "license": "MIT",
  "scripts": {
    "start": "node dist/index.js",
    "prebuild": "rimraf dist",
    "build": "npm-run-all -s clean generate lint build:*",
    "build:tsc": "tsc",
    "postversion": "git push && git push --tags",
    "test": "jest",
    "lint": "eslint . -c .eslintrc.js --ext .ts,.tsx --cache",
    "clean": "npm-run-all -s clean:*",
    "clean:dist": "rimraf dist",
    "clean:yaml-files": "rimraf generated/yaml-file/*.yaml",
    "clean:ts-from-yaml": "rimraf src/generated/api",
    "generate": "npm-run-all -s generate:ts-from-yaml:*",
    "generate:ts-from-yaml:api": "shx mkdir -p src/generated/api && rimraf src/generated/api/* && gen-api-models --strict 0 --api-spec ./api.yaml --out-dir src/generated/api"
  },
  "devDependencies": {
    "@pagopa/eslint-config": "^1.3.1",
    "@types/express": "^4.17.6",
    "@types/jest": "^25.2.1",
    "@types/node": "10.14.1",
    "@types/winston": "^2.4.4",
    "danger": "^8.0.0",
    "danger-plugin-digitalcitizenship": "*",
    "eslint-plugin-prettier": "^4.0.0",
    "jest": "^25.2.7",
    "npm-run-all": "^4.1.3",
    "prettier": "^1.12.1",
    "rimraf": "^2.6.2",
    "shx": "^0.3.3",
    "ts-jest": "^25.3.1",
    "tslint": "^5.1.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@pagopa/openapi-codegen-ts": "^10.0.6",
    "@pagopa/ts-commons": "^10.2.0",
    "express": "^4.17.1",
    "fp-ts": "^2.11.1",
    "http-proxy-middleware": "^2.0.1",
    "io-ts": "^2.2.16",
    "io-ts-reporters": "^2.0.0",
    "parcel-bundler": "^1.12.5",
    "winston": "^3.2.1"
  }
}
